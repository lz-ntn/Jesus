{
  "name": "jesus-sem-filtro",
  "version": "1.0.0",
  "description": "Blog sobre Jesus histórico - sem filtros religiosos",
  "type": "module",
  "scripts": {
    "dev": "node scripts/build.js && vite",
    "build": "node scripts/build.js && vite build && node scripts/postbuild.js",
    "preview": "vite preview",
    "start": "serve dist --no-clipboard"
  },
  "devDependencies": {
    "dompurify": "^3.1.0",
    "glob": "^11.0.0",
    "gray-matter": "^4.0.3",
    "jsdom": "^24.0.0",
    "marked": "^14.0.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "serve": "^14.2.6"
  }
}
